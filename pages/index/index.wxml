<!--pages/index/index.wxml-->
<text class="top-tip">{{updateTime}}更新</text>
<view class="wrapper">
  <!-- 最新50条热搜 -->
  <!-- <text class="update-time">{{updateTime}}更新</text> -->
  <view>
    <view 
      class="list-item" 
      wx:for="{{hotList}}" 
      wx:key="desc"
      data-desc="{{item.desc}}"
      bindtap="jumpToDetail">
      <text class="item-index">{{index + 1}}</text>
      <text class="item-desc">{{item.desc}}</text>
      <text class="item-n">{{item.n}}</text>
    </view>
  </view>
</view>
<text class="bottom-tip">人是会思考的芦苇</text>
